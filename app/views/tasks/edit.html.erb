<h1>id: <%= @tasks.id %> の編集ページ</h1>

    <%= form_for(@tasks) do |f| %>
      <%= f.label :cotent, 'タスク' %>
      <%= f.text_field :cotent %>

    <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>